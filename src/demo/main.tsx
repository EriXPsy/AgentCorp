import React from 'react';
import ReactDOM from 'react-dom/client';
import ClosedLoopDemo from './ClosedLoopDemo';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ClosedLoopDemo />
  </React.StrictMode>,
);
