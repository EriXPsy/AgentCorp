# 概述

<!-- 一句话说明本 PR 做了什么、为什么（关联 issue/需求背景） -->

## 三级归属声明（必填）

<!-- 本次改动涉及（填 Y 或 N）：

- L1 域入口（域 index.ts / 域文档头）：Y / N
- L2 契约（contracts.ts / types.ts 中的跨域消费类型）：Y / N
- L3 实现细节（域内实现文件）：Y / N
- 新增域（登记进 scripts/qa/l1-domains.json）：Y / N
-->

## 七准则评估（仅拆分/重构类 PR 必填，其余填"不适用"）

<!-- 每行填：支持 / 反对 / 不适用，并给一句依据。准则 1/7 为一票否决项。 -->

| # | 准则 | 判定 | 依据 |
|---|------|------|------|
| 1 | 功能能否独立完成（有独立输入输出闭环） | | |
| 2 | 内部函数共享同一数据流 | | |
| 3 | 独立外部调用边界（≥1 外部消费方） | | |
| 4 | 独立测试契约（可单独断言） | | |
| 5 | 不同的修改原因 | | |
| 6 | 拆分降低理解审计成本 | | |
| 7 | 不新增调度复杂度（文件数 ≤ 域上限，无新机制） | | |

<!-- 关键决策判断依据（为何拆/不拆，一句话）： -->

## 验证清单（必填，全过才可合并）

- [ ] `pnpm typecheck` — 0 错误
- [ ] `pnpm test`（vitest）— 全过
- [ ] `pnpm lint:check` — 0 error
- [ ] `node scripts/qa/size-budget.mjs` — 体积棘轮通过（只减不增）
- [ ] `node scripts/qa/dep-ratchet.mjs` — 依赖棘轮通过（只减不增）
- [ ] `node scripts/qa/l1-doc-check.mjs` — L1 域文档检查通过
- [ ] `pnpm privacy:check` — CLEAN

## 品控声明（最小冗余红线）

<!-- 本 PR 每个新增文件拦截的回归类型（typecheck / vitest 套件名 / 冒烟场景）：
无对应回归场景的抽象 = 过度设计，打回。 -->

## 评审清单（Reviewer Checklist）

- [ ] 只读该域 index.ts，能复述域职责与核心行为（30 秒测试）
- [ ] 新增跨域类型进 L2 契约文件，未散落实现文件
- [ ] 未引入新的深路径 import（域外 → L3 直连）
- [ ] index.ts re-export ≤ 20 符号
- [ ] 拆分类 PR 的七准则评估表齐全且关键判断有依据
- [ ] 每个新文件声明了它拦截的回归类型
